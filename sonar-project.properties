# SonarCloud configuration
# Verify these two keys match what SonarCloud shows when you import the repo.
sonar.projectKey=manitejakanuri1_gmail-intergents
sonar.organization=manitejakanuri1

# Code to analyze
sonar.sources=backend/app,frontend/src

# Exclude dependencies, builds, and generated files
sonar.exclusions=**/node_modules/**,**/.venv/**,**/dist/**,**/__pycache__/**,**/*.tsbuildinfo

sonar.python.version=3.11,3.12,3.13,3.14
sonar.sourceEncoding=UTF-8
