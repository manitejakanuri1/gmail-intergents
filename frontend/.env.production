# Baked into the production build. Points the SPA at the deployed backend.
# (Public config, not a secret.) Update + redeploy if the Render URL differs.
VITE_API_URL=https://gmail-intelligence-api.onrender.com
